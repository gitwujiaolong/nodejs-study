/* 
  const request = require('request')
  const iconv = require('iconv-lite')

  const html = iconv.decode('xxx','gb2312')
  const cheerio = require('cheerio') // nodejs 端的jquery
  const $ = cheerio.load(html)
  console.log($('.title_all').text())
*/